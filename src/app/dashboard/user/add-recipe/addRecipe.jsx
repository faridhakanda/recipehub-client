import React from 'react';

const AddRecipe = ({ user }) => {
    console.log('user', user);
    return (
        <div>
            <h2>Add Recipe for User</h2>
        </div>
    );
};

export default AddRecipe;